# 2026-06-30 工作日志

## 今日完成

1. 给后端会话历史接入了真正的 PostgreSQL 持久化，不再只停留在内存态。
   - 修正了 `SESSION_HISTORY_DB_URL` 的环境变量配置。
   - 验证了 `SessionHistoryStore` 可以正常连到服务器上的 `travel_agent` 库。

2. 把前端会话列表和会话切换接到了后端真实数据。
   - 会话列表从后端 `/api/sessions` 拉取。
   - 切换会话时从后端 `/api/session/{session_id}` 还原消息历史。
   - 这一步让前端不再只是 mock，而是有真实会话生命周期。

3. 接入了高德 MCP，并保留了本地/公开接口兜底。
   - 地点搜索、地理编码、路线规划、天气查询优先走高德 MCP。
   - 如果 MCP 不可用，会自动退回到 OpenTripMap、Nominatim、OSRM、Open-Meteo 等公开数据源。

4. 给高德 MCP 加了全局限流。
   - 按 3 次/秒做滑动窗口控制。
   - 避免把高德开放平台的 QPS 打爆。

5. 做了基本的启动校验。
   - 后端 Python 文件可通过语法检查。
   - 数据库连接可用。

## 当前系统状态

- 后端已经具备会话历史持久化能力。
- 前端已经支持真实会话切换。
- 高德 MCP 已经接入，但会在限流或异常时自动降级。
- 系统主干仍然是 workflow-based agent，核心调度是 LangGraph 状态机，不是为了追潮流硬改成 ReAct。

## 今天的结论

这套系统现在不是“只有壳”，而是已经开始具备 agent 平台该有的骨架：

- 能保留会话
- 能恢复上下文
- 能接外部工具
- 能做降级兜底
- 能约束调用频率

后续真正拉开档次的地方，不是再堆一个新工具，而是把上下文工程、记忆分层、评测闭环和 harness/sandbox 这一层继续做扎实。
